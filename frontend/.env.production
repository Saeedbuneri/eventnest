# This file is committed intentionally.
# It forces mock/demo mode on ALL production builds (Vercel, Netlify, etc.)
# so the frontend works without a running backend.
# To connect a real backend: set NEXT_PUBLIC_API_URL in your host's env var UI
# and remove or override NEXT_PUBLIC_USE_MOCK=false there.
NEXT_PUBLIC_USE_MOCK=true
